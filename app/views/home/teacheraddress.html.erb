<%= render "layouts/main-nav" %>
 
<div class="container py-5 dash-view mt-4">
  <div class="row">
    <div class="col-md-4 pr-5">
      <div class="mb-5">
        <div class="text-center" style="font-size: 6em;">👩🏽‍🏫</div>

        <!-- <div class="profile-pic text-center">
          <% if current_teacher.image.attached? %>
            <%= image_tag current_teacher.image, style: "width: 200px; display: block; margin: 0 auto", class: "rounded-circle shadow" %>
          <% end %>
        </div> -->

        <p class="text-center mt-5 text-muted">
          <%= current_teacher.name %>
        </p>
      </div>
    </div>
    
    <div class="col-md-8">
      <%= render 'layouts/notice' %>

      <h2 class="font-weight-bold text-heading pb-3">My Address</h2>
      <div class="">
        <% if direction_add %>
          <div class="row">
            <div class="col-md-12">
              <p class="profile-p">
                <strong>Nearest Landmark:</strong><br>
                <span>
                  <%= @direction.landmark %>
                </span>
              </p>
            </div>
            <div class="col-md-12 col-lg-6 pt-2">
              <%= link_to '<i class="fas fa-pencil-alt mr-2"></i> Update Address'.html_safe, edit_direction_path(@direction), class: "btn btn-info px-5 py-2 mt-2" %>
            </div>
          </div>
        <% else %>
          <p class="profile-p text-center">
            <span>
              <strong>
                <%= link_to '+ Add an Address', new_direction_path, class: 'orange-link' %>
              </strong>
            </span>
          </p>
        <% end %>
      </div>
    </div>
  </div>
</div>
